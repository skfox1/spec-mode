openapi: 3.0.3
info:
  title: Complex Ref Test API
  version: 1.0.0
paths:
  /complex-users:
    get:
      tags:
        - Complex Refs
      operationId: listComplexUsers
      summary: List users with circular team references
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: './components/schemas/circular-user.yaml'

